#!/bin/bash

ip address add {{ secondNetIf_ip }}/24 dev {{ secondNetIf_name }}
ip address add {{ thirdNetIf_ip }}/24 dev {{ thirdNetIf_name }}
ip link set dev {{ secondNetIf_name }} up
ip link set dev {{ thirdNetIf_name }} up

exit 0